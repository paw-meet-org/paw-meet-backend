services:
  postgres:
    container_name: pawmeet_db
    image: postgres:18
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pawmeet_db
    volumes:
      - postgres_data:/var/lib/postgresql

  pgadmin:
    image: dpage/pgadmin4:latest
    restart: unless-stopped
    ports:
      - "8081:80"
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@example.com
      - PGADMIN_DEFAULT_PASSWORD=admin
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres

  pawmeet:
    image: mendo1/paw_meet_backend:1.0
    container_name: django_pawmeet
    restart: always
    volumes: 
      - ./paw_meet:/app
    env_file:
      - ./paw_meet/.env
    depends_on:
      - postgres
    ports: 
      - "8003:8000"


  pawmeet_develop:
    build: .
    container_name: django_pawmeet_develop
    restart: always
    volumes:
      - ./paw_meet_develop:/app
    env_file:
      - ./paw_meet/.env
    depends_on:
      - postgres
    ports:
      - "8004:8000"
    profiles:
      - desarrollo
    expose:
      - 8000

volumes:
  postgres_data:
  pgadmin_data: 